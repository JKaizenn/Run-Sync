specVersion: "v1beta"
serviceId: "run_sync"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "run_sync-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
